services:
  db-primary:
    image: bitnami/postgresql:latest
    environment:
      - POSTGRESQL_USERNAME=app
      - POSTGRESQL_PASSWORD=app_pwd
      - POSTGRESQL_DATABASE=appdb
      - POSTGRESQL_REPLICATION_MODE=master
      - POSTGRESQL_REPLICATION_USER=repl
      - POSTGRESQL_REPLICATION_PASSWORD=repl_pwd
    ports:
      - "5432:5432"

  db-replica:
    image: bitnami/postgresql:latest
    depends_on:
      - db-primary
    environment:
      - POSTGRESQL_USERNAME=app
      - POSTGRESQL_PASSWORD=app_pwd
      - POSTGRESQL_DATABASE=appdb
      - POSTGRESQL_REPLICATION_MODE=slave
      - POSTGRESQL_MASTER_HOST=db-primary
      - POSTGRESQL_MASTER_PORT_NUMBER=5432
      - POSTGRESQL_REPLICATION_USER=repl
      - POSTGRESQL_REPLICATION_PASSWORD=repl_pwd
    ports:
      - "5433:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  haproxy:
    image: haproxy:2.9
    depends_on:
      - db-primary
      - db-replica
    ports:
      - "5439:5432"
    volumes:
      - ./haproxy:/usr/local/etc/haproxy
  api:
    build: ./partieD
    ports:
      - "3000:3000"
    depends_on:
      - db-primary
    environment:
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=app_pwd
      - POSTGRES_DB=appdb
      - POSTGRES_HOST=db-replica
      - POSTGRES_PORT=5432
      - POSTGRES_HOST2=haproxy
      - REDIS_HOST=redis
      - REDIS_PORT=6379
